/*#include <stdio.h>

int main()
{
	double tran, len, area, dul;
	printf("사각형 가로 입력 : ");
	scanf_s("%lf", &tran);
	printf("사각형 세로 입력 : ");
	scanf_s("%lf", &len);
	area = tran * len;
	dul = 2 * (tran + len);
	printf("사각형 가로 %lf, 세로 %lf의 면적은 %lf이고, 둘레는 %lf입니다.", tran, len, area, dul);
	return 0;
}*/